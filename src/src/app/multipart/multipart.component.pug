form(action="/api/sampleMultipart", enctype="multipart/form-data", method="POST", ngNoForm)
  div
    h1 File Upload
    input(type="file", name="picture")
  div
    input(type="submit", value="Upload")
